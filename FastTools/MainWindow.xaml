<Window x:Class="FastTools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FastTools" Height="480" Width="900">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
                <RowDefinition Height="2*" />
            </Grid.RowDefinitions>

            <!-- ADB设备显示区域 -->
            <Border Grid.Row="0" VerticalAlignment="Stretch" BorderBrush="LightGray" BorderThickness="1" CornerRadius="15" Margin="0,0,0,10">
                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="已连接设备" FontWeight="Bold" Margin="0,0,0,8" />
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="DevicePanel" />
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- 按钮面板 -->
            <Border Grid.Row="1" VerticalAlignment="Stretch" BorderBrush="LightGray" BorderThickness="1" CornerRadius="15">
                <Grid Margin="10 5 0 5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <TextBlock Text="常用命令" FontWeight="Bold" />
                        <Button Name="AddCommandButton" Width="24" Height="24" HorizontalAlignment="Right" VerticalAlignment="Top" Background="Transparent" BorderBrush="Transparent" Padding="0" Click="AddCommandButton_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border CornerRadius="4" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Content="{TemplateBinding Content}" />
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                            <TextBlock Text="➕" FontFamily="Segoe UI Emoji, Segoe UI Symbol, Microsoft YaHei" FontSize="14" Foreground="Gray" />
                        </Button>
                    </Grid>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0 0 5 0">
                        <StackPanel x:Name="BtnPanel" />
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- 输出面板 -->
        <Border Grid.Column="1" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="40" />
                </Grid.RowDefinitions>

                <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                    <Grid>
                        <StackPanel x:Name="OutputPanel" />
                        <Button Click="BtnClear_Click" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,10" Width="30" Height="30" Background="Transparent" BorderBrush="Transparent">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="8" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Content="{TemplateBinding Content}" />
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <TextBlock Text="🗑️" FontFamily="Segoe UI Emoji, Segoe UI Symbol, Microsoft YaHei" FontSize="16" Foreground="Gray" />
                </Button>
                    </Grid>
                </ScrollViewer>

                <StackPanel Grid.Row="1" Orientation="Vertical" Margin="8,8,0,0">
                    <TextBlock x:Name="AdminNote" FontWeight="Bold" Text="若命令需要管理员权限，请以管理员方式运行本程序。" TextWrapping="Wrap" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
